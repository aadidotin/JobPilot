# Copy to .env (gitignored) and fill in.
TELEGRAM_BOT_TOKEN=        # from @BotFather
TELEGRAM_CHAT_ID=          # your own chat id (message the bot, then hit getUpdates)
JOBPILOT_DB=data/jobpilot.db   # data/ is gitignored — the DB holds third-party PII
# ANTHROPIC_API_KEY=       # weekend 2+ (Haiku scorer)
# HEALTHCHECKS_PING_URL=   # dead-man's-ping, healthchecks.io free tier
